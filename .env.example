# Snowflake Connection (passed via frontend, not stored server-side)
# These are example values - real credentials come from the UI

# API Configuration
API_PORT=3001
API_HOST=localhost

# Frontend Configuration
VITE_API_URL=http://localhost:3001
